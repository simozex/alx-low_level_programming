#include "main.h"

/**
 * _abs - computes the absolute value of a given integer.
 * @n: fo _abs input
 *
 * Return: always stays n success
 */

int _abs(int n)
{
	if (n < 0)
	{
		n *= -1;
	}
	return (n);
}
